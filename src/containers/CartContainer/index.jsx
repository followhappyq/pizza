import React from "react"

import CartComponent from "../../components/CartComponent"

const CartContainer = () => {
  return <CartComponent total={0} cartCount={0} />
}

export default CartContainer
